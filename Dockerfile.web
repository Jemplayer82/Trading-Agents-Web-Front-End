# Pure static frontend: nginx serving the dashboard HTML/CSS/JS and
# reverse-proxying API + WebSocket traffic to the agents API + portfolio
# services. No Python, no secrets in this image.
FROM nginx:alpine

COPY web/nginx.conf /etc/nginx/nginx.conf
COPY web/static/index.html /usr/share/nginx/html/index.html
# Copy the whole static dir so newly-added JS/CSS files don't get
# dropped (e.g. spy.js, credentials.js were silently 404→SPA-fallback
# before this).
COPY web/static/ /usr/share/nginx/html/static/

EXPOSE 8000
